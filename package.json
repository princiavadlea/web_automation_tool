{
  "name": "auto-tester-demo",
  "version": "1.0.0",
  "description": "Automated security and UI testing tool for web applications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && npx playwright install chromium",
    "postinstall": "npx playwright install chromium --with-deps 2>/dev/null || echo 'Playwright browser install skipped (run: npx playwright install chromium)'"
  },
  "dependencies": {
    "@swc/helpers": "^0.5.0",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "iconv-lite": "^0.7.2",
    "node-html-parser": "^6.1.0",
    "nodemailer": "^8.0.3",
    "pdfkit": "^0.14.0",
    "playwright": "^1.40.0",
    "undici": "^7.24.4",
    "uuid": "^9.0.0"
  }
}
